server:
  port: 8888

logging:
  path: logs
  file: mylog.log

spring:
  application:
    name: myTest
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_database
    username: root
    password: root

mybatis:
  type-aliases-package: com.example.sqlModel
  mapper-locations:
    - mapper/*